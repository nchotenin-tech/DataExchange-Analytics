# ไลบรารีที่จำเป็นตอนรันจริง (ใช้ตอน build exe ด้วย)
pandas>=2.0
openpyxl>=3.1
PyYAML>=6.0
Flask>=3.0

# pyarrow เป็นตัวเลือก — ถ้ามีจะ cache เป็น .parquet (เร็วกว่าเล็กน้อย)
# ถ้าไม่มีจะใช้ .pkl แทน ผลลัพธ์เหมือนกัน
# ไม่ใส่ใน exe เพราะทำให้ไฟล์ใหญ่ขึ้นราว 100 MB
